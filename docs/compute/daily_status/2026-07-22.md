# Compute daily status — 2026-07-22 UTC

Status captured: 2026-07-22 11:20 UTC

Task scope: `P0-WHY-NOT-01`, control-plane validation, source/environment audit

## Node status

| Node | Registered role | CPU/RAM/GPU evidence | Storage evidence | Uncertainty activity |
|---|---|---|---|---|
| `vultr-research` | shared control only | no heavy scientific compute authorized | project control/state repositories healthy | Codex, status service, ledger, repository and CI coordination |
| `autodl-westb-01` | elastic worker | captured allocation: 25 effective logical CPUs, 92 GiB memory limit, and host-visible RTX 4090 vGPU label with 49,140 MiB; the label is not a physical-card or FP64-performance guarantee; latest workload was CPU-only | capture reports 50 GiB rebuildable `/root/autodl-tmp` and 200 GiB regional persistent `/root/autodl-fs` | W1 and W3 completed; no uncertainty runner remains and the `cpu-heavy` lease was released |
| `autodl-bjb1-01` | elastic worker | uncertainty hardware acceptance pending | regional persistent root present; uncertainty project bootstrap deferred | unavailable because external workloads are active; no uncertainty lease or process started |
| `autodl-bjb1-02-spare` | optional elastic worker | uncertainty hardware acceptance pending | regional persistent root present; uncertainty project bootstrap deferred | unavailable because external services are active; no uncertainty lease or process started |

No public endpoint, password, token, SSH key, or other-project run detail is
recorded in this status file.

## Latest completed run

| Run | Baseline | State | Progress at capture | Failures | Persistence |
|---|---|---|---:|---:|---|
| `run-20260722T1118Z` | W3 ABCMB bundled-LINX standard-fiducial runtime | complete | 30/30 scalar and 30/30 native 64-point batches; 1,950/1,950 warm points | 0 | persistent worker copy plus validated repository artifact; integrity report passed 21 checks |

W1 completed uninterrupted under `nohup` and an exclusive host `cpu-heavy`
lease. Its legacy runner preserved batch-level records but did not itself have
restart semantics. The lease has been released. The final manifest, timing,
failure, posterior-boundary and resource files passed the generic integrity
validator and are registered at runtime-slice scope only. The replacement
runner now used for future repeated runtime campaigns has a tested resume
contract; that fact does not retroactively change how W1 executed.

W3 then completed under the same host-level lease policy and released its lease.
Its scalar median was `0.31112 s`; the native 64-point median was `0.89676 s`
or `0.01401 s/point`. The maximum scalar-versus-batch abundance difference was
`1.5777656725834976e-6`, dominated by `Y_p`, so the batch discrepancy remains open.
This component slice does not accept the full ABCMB CMB/Fisher/HMC pipeline.

## Completed registered evidence

- W2 PRIMAT standard-fiducial runtime: complete, 1,950 warm points, zero failures;
- W0 LINX standard-fiducial runtime: complete, 1,950 warm points, zero runtime failures;
- LINX tolerance scan V1: complete, both registered plateaus failed;
- LINX extended convergence V2: complete with five structured `max_steps=4096` failures;
- LINX max-step diagnostic V3: complete with the registered 4096 control failure;
- LINX convergence rerun V4: complete, standard-point numerical candidate passed;
- W0 and W2 runtime artifacts: passed the generic 21-check integrity validator.
- W1 PRyMordial standard-fiducial runtime `run-20260722T0753Z`: complete, 1,950 warm points, zero failures, and passed the generic 21-check integrity validator.
- W3 ABCMB bundled-LINX standard-fiducial component runtime `run-20260722T1118Z`: complete, 1,950 warm points, zero failures, and passed the generic 21-check integrity validator; scalar/batch discrepancy remains open.

These are standard-point numerical/runtime labels only. No posterior, gradient,
rate-nuisance, extension, SBC or flagship-physics label is accepted.

## Completed cost ledger

The eight completed runs recorded in `docs/compute/cost_ledger.csv` total:

- worker time: `3.846094010918815` hours;
- measured CPU process time: `4.0754905519175` core-hours;
- GPU time: `0` hours;
- estimated worker cost: `CNY 11.076750751446188`.

The W1 cost is included from its validated resource report. Storage subscription
and network-transfer charges are not present in the per-run reports and are
therefore excluded rather than estimated.

## Checkpoint and backup status

- completed small artifacts are committed to GitHub `main` with config and lock hashes;
- the completed W1 artifact and log remain on regional persistent storage;
- the validated W1 evidence set is copied into the repository;
- the validated W3 evidence set is copied into the repository;
- future repeated runtime campaigns have a tested resume contract;
- all observed research-worker leases from completed uncertainty runs were released;
- Tailscale heartbeat from westb remains unavailable, so process state is verified over control-to-worker SSH.

## Blocking issues and next decision

1. Preserve W1 and W3 as runtime-only results; do not infer posterior, functional-rate, extension or production acceptance from them.
2. Design the registered W3 scalar/native-batch numerical follow-up before any gradient, Fisher or HMC claim.
3. Use the tested restart/resume runner for future multi-hour repeated runtime campaigns.
4. Do not use either bjb1 node while external workloads/services remain active.
5. Modified PArthENoPE/AlterBBN and old BBNet/MCMC assets remain separate scientific blockers; they do not block W1/W3 component runtime work.
