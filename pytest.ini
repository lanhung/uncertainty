[pytest]
# Compatibility shim for tools that do not read pyproject.toml. Keep these
# values aligned with [tool.pytest.ini_options].
testpaths = tests
norecursedirs = legacy
pythonpath = .
addopts = -ra
